/** Z. Consulting wordmark. Swap the `.mark` for a real logo when available. */
export function Brand() {
  return (
    <a className="brand" href="#top" aria-label="Z. Consulting">
      <span className="mark">Z.</span>
      <span>
        Consulting<span className="sub"></span>
      </span>
    </a>
  );
}
